INPUT SECTION:
- auto-fill in sprinkler (can only do after data collected from calendar)

CALENDAR:
- build square (holds data for each square)
- updates output data object

OUTPUT SECTION:
- uses useroptions and output data object to update output